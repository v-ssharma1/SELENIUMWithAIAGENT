# Development Environment Configuration

# Environment name
environment=dev

# Base URL for dev
url=https://www.google.com

# Browser type
# Supported values: chrome, firefox, edge, safari, headless-chrome, headless-firefox
browser=chrome

# Wait times for dev (shorter timeouts, faster response expected)
implicitWait=10
explicitWait=20
