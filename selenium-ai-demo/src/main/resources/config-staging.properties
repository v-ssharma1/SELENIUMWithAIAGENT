# Staging Environment Configuration

# Environment name
environment=staging

# Base URL for staging
url=https://staging.google.com

# Browser type
# Supported values: chrome, firefox, edge, safari, headless-chrome, headless-firefox
browser=headless-chrome

# Wait times for staging (moderate timeouts)
implicitWait=15
explicitWait=25
